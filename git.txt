ADICIONANDO ARQUIVO NO GIT:

git add "file"

git status

git commit -m "comentario"

git push origin main


git rebase -i hash~n

git log --oneline

git log arq

git diff (sem commitar)

git diff --staged (commitado)

git rm --cached arq

git show hash
